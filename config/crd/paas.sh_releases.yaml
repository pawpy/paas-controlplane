---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: releases.paas.sh
spec:
  group: paas.sh
  names:
    kind: Release
    listKind: ReleaseList
    plural: releases
    singular: release
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.app
      name: App
      type: string
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .spec.gitSHA
      name: SHA
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Release is one immutable deploy of an App.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ReleaseSpec is an immutable record of one deploy: an app + the image built
              for a given git sha. Creating a Release triggers a rollout of its App.
            properties:
              app:
                description: App is the name of the App this release belongs to (same
                  namespace).
                type: string
              gitSHA:
                description: GitSHA is the commit this release was built from.
                type: string
              image:
                description: Image is the OCI ref produced by the build (e.g. registry.local/<app>:<sha>).
                type: string
            required:
            - app
            - image
            type: object
          status:
            description: ReleaseStatus is the observed state.
            properties:
              phase:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
